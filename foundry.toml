[profile.default]
src = "src"
out = "out"
libs = ["lib"]

remappings = [
    "@chainlink/contracts/=lib/chainlink-brownie-contracts/contracts/",
]
ffi = true #允许foundry直接在计算机上执行命令，即运行bash脚本(一般建议尽量不要打开)

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
